[package]
name = "felix"
version = "0.1.0"
edition = "2024"
description = "A lean, fast, GPU-accelerated code editor"
license = "MIT"

[dependencies]
gpui = "0.2"
ropey = "1"
tree-sitter = "0.26"
tree-sitter-rust = "0.24"
tokio = { version = "1", features = ["full"] }
